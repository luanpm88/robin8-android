<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充颜色 -->
    <solid android:color="@color/undefined_backgroud"></solid>

    <!-- 线的宽度，颜色灰色 -->
    <stroke android:width="0.4dp" android:dashWidth="6dp" android:dashGap="4dp" android:color="@color/sub_gray_custom"></stroke>

</shape>
